# GitHub 上傳步驟

## 📋 前置檢查

✅ 檔案已清理完成  
✅ .gitignore 已建立  
✅ README.md 已更新  
✅ 程式可正常執行  

---

## 🚀 上傳步驟

### 步驟 1: 初始化 Git Repository

在專案資料夾中開啟 PowerShell 或 CMD，執行：

```bash
git init
```

### 步驟 2: 加入所有檔案

```bash
git add .
```

### 步驟 3: 建立第一次 Commit

```bash
git commit -m "Initial commit: TMflow Modbus Testkit v1.0.1.0001

- 完整的 GUI 測試工具
- 預設測試功能 (Base, Tool, Joint, Status, User Define)
- 自定義測試功能
- 性能測試功能
- 整合連線/斷線按鈕
- 日誌管理
- 連續監控
- 測試報告生成"
```

### 步驟 4: 連結到 GitHub Repository

將 `你的帳號` 替換為你的 GitHub 使用者名稱：

```bash
git remote add origin https://github.com/你的帳號/Modbus-testkit-for-TMflow.git
```

或使用你在 GitHub 上建立的 repository 名稱。

### 步驟 5: 推送到 GitHub

```bash
git branch -M main
git push -u origin main
```

---

## 📝 GitHub Repository 設定建議

### Repository 資訊

**Name**: `Modbus-testkit-for-TMflow`

**Description**: 
```
TMflow Modbus 功能驗證工具 - 測試 TMflow 的 Modbus TCP 讀寫是否符合 TM Robot 規範
```

或英文版：
```
TMflow Modbus Testing Tool - Verify TMflow Modbus TCP read/write compliance with TM Robot specifications
```

**Topics** (標籤):
```
modbus, tmflow, tm-robot, testing, python, tkinter, modbus-tcp, automation, gui, test-automation
```

### 可見性

- **Public**: 開放給所有人查看
- **Private**: 僅限你和協作者查看

---

## 🔧 如果遇到問題

### 問題 1: 已經有 .git 資料夾

```bash
# 刪除舊的 .git 資料夾
Remove-Item -Recurse -Force .git

# 重新初始化
git init
```

### 問題 2: 推送被拒絕

```bash
# 強制推送（小心使用）
git push -u origin main --force
```

### 問題 3: 需要登入

GitHub 可能會要求你登入：
- 使用 GitHub Desktop（推薦）
- 使用 Personal Access Token
- 使用 SSH Key

---

## 📦 後續維護

### 更新程式碼

```bash
# 查看修改
git status

# 加入修改
git add .

# 提交修改
git commit -m "描述你的修改"

# 推送到 GitHub
git push
```

### 建立新版本標籤

```bash
git tag -a v1.0.1.0001 -m "Release v1.0.1.0001"
git push origin v1.0.1.0001
```

---

## ✨ 完成後

1. 在 GitHub 上查看你的 repository
2. 確認所有檔案都已上傳
3. 檢查 README.md 是否正確顯示
4. 可以分享連結給其他人了！

---

**Repository URL 範例**:
```
https://github.com/你的帳號/Modbus-testkit-for-TMflow
```

🎉 恭喜！你的專案已經上傳到 GitHub！
